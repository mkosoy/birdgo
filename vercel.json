{
  "buildCommand": "cd backend && npm install && npm run build && cd ../mobile && npm install && npx expo export --platform web",
  "outputDirectory": "mobile/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
